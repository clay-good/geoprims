# Tasks

- [x] 1 Move the chain runner to `packages/runtime/src/chain.mjs`, with `each` rows and per-step errors → verify: `journeys.test.mjs` passes unchanged against the moved runner, and a new test stops a chain at a failing step with its error
- [x] 2 `data/workflows.json` schema and lint: inputs map onto real step inputs, `from` points backward to real outputs, at most 8 visible inputs → verify: `tools/trust/workflows.test.mjs` fails on each broken fixture (built: the checks live in `apps/web/test/workflows.test.mjs`: every input names a real tool field and feeds a step, every step input is one its tool takes, at most 8 visible inputs, a real visual step; wiring mistakes throw naming the step)
- [x] 3 Port the 8 journeys into workflows (vfr-preflight → preflight-check, drone-mapping-day → mapping-flight, the rest by slug); add `/journeys/<slug>/` → `/workflows/<slug>/` redirects → verify: `routes.test.mjs` and `redirects.json` check; the build runs every workflow's prefill through the core
- [x] 4 Workflow page: form, step list with answer sentences and tool links, assumptions, live recompute, permalink → verify: a browser test edits one input on `vfr-cross-country` and checks every later step's sentence changes, and the permalink restores it (built: `WorkflowApp.svelte`, server-rendered with the example and rerun live; `test/browser/workflow.test.mjs` edits the cross-country's TAS, checks steps 2–4 change and step 1 does not, reopens the permalink, and checks a bad METAR stops at step 1 with the rest waiting)
- [ ] 5 Combined visuals of design D4 → verify: `visual-purpose.test.mjs` extended to workflows (each draws from its prefill) (built: each workflow draws its key step's own diagram, else its map, gated in `visual-purpose.test.mjs`. Pending: the richer combined views — sorties colored on the mapping flight, TOC/TOD on the cross-country — which need the new tools)
- [ ] 6 Exports: nav log CSV and print view; mission KML; coordinate table copy → verify: export tests byte-compare the prefill's files (built: "Copy the plan" on every workflow — the inputs and each step's own sentence, tested in `workflows.test.mjs`. Pending: nav log CSV and mission KML, after the new tools)
- [x] 7 MCP: `workflow.<slug>` in search, describe, and run → verify: golden surface updated; `mcp/server.test.mjs` runs each workflow and matches the web chain byte for byte
- [ ] 8 Build the new-tool workflows (vfr-cross-country, mapping-flight, can-i-fly-now) after `add-flight-and-drone-planning-tools` lands → verify: tasks 4 to 7 for each
- [ ] 9 Performance: the whole mapping-flight chain within 300 ms on the reference profile → verify: `bench:browser` case
