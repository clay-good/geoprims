{
  "name": "@geoprims/web",
  "private": true,
  "type": "module",
  "description": "The geoprims static website (Astro + Svelte islands).",
  "scripts": {
    "prepare-assets": "node scripts/prepare-assets.mjs",
    "build": "npm run prepare-assets && node scripts/examples.mjs && astro build && node scripts/og.mjs && node scripts/links.mjs && node scripts/discovery.mjs && node scripts/routes.mjs && node scripts/headers.mjs && node scripts/pwa.mjs",
    "dev": "npm run prepare-assets && astro dev",
    "preview": "astro preview",
    "test": "node --test 'test/*.test.mjs'",
    "test:browser": "node --test --test-concurrency=1 'test/browser/*.test.mjs'",
    "bench:browser": "node scripts/benchmark.mjs"
  },
  "devDependencies": {
    "@astrojs/svelte": "9.0.1",
    "astro": "7.3.3",
    "axe-core": "4.13.0",
    "playwright": "1.62.1",
    "svelte": "5.57.0"
  }
}
