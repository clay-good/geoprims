## 1. Anatomy and answer card

- [x] 1.1 Build the fixed tool-page layout with the answer card above inputs and sticky on phones; verify the answer-first and anatomy scenarios (the anatomy gate already held the region order; the answer now precedes the inputs on every tool page with a measured budget on what may stand above it, and a tool whose meaning is a picture — the five that declare `x-diagram-inline` — draws a compact one directly under the answer as well as the full canvas below, with each drawing's marker ids its own)
- [x] 1.2 Build the answer card (value, sentence, comparison, warnings, status phrase, copy actions); verify the density-altitude sentence scenario (the card shows the value, the core-rendered sentence, the comparison line, status phrases, severity-ordered warnings, and copy actions. The scenario is pinned end to end: the card reads "7,932 ft", the sentence names the 2,900 ft above the field, and the comparison reads "The 120 ft per °C rule of thumb gives 8,123 ft (191 ft high).", with the more exact 118.8 ft/°C rule among the details)
- [ ] 1.3 Add `x-sentence` templates and comparison types to every stable manifest, with the readability lint; verify the template-coverage and MCP-summary scenarios (done so far: the core renderer, the template lint, the grade-8 and 280-character lint over every example, and `summary` in every result; comparison types are declared as x-comparison and rendered by the core, so the web page and MCP show the same line)
- [x] 1.4 Implement status phrases with icons, cited thresholds, and a banned-word lint; verify the crosswind scenario (the core renders the five allowed phrases from one shared generator; outputs declare `x-status` with the threshold's source, which the meta-schema gate checks; the answer card shows each as its own line with a mark and the cited source, never colour alone, and keeps it out of the facts list; tools/trust/status.mjs runs every example of every status tool and fails on a phrase that is not one of the allowed shapes or that says safe, unsafe, legal, or approved. The crosswind scenario is pinned at Within, Near, and Beyond)
- [x] 1.5 Implement prefilled examples with the "Example values" chip, Clear, and Try the example; verify the first-load scenario (every tool page ships filled from its worked example with the answer already in the HTML, labelled until the first edit; Clear empties every field and Try the example puts them back, gated over the whole catalog by `apps/web/test/example.test.mjs`)
- [x] 1.6 Implement progressive disclosure with worded defaults; verify the dry-air scenario (five inputs at most before "More options" and no required input hidden behind it; what a tool assumed in place of an input it did not get is stated on the page as its `x-limitation` banner, and `apps/web/test/disclosure.test.mjs` checks the dry-air case by name, that every declared limitation is shown and every shown one is declared, and that a remedy naming an input names one the tool has)
- [x] 1.7 Build the glossary file and tap-to-define popovers, plus the help-text gate; verify the HAE scenario (an abbreviation in a tool's purpose line is now a button opening a native popover with its expansion, definition, and the ledger source it cites — no script, so it works offline and with JavaScript off — and the terms list cites its sources too; `apps/web/test/terms.test.mjs` holds the 40-word cap, the first-use-only rule, the escaping, the button-to-popover pairing on every page, and one line of help with an example value on every input, including the fields inside a list's rows)

## 2. Home page

- [x] 2.1 Build the glanceable home (question search, hero cards with no-JS answers, journeys, recents, four promises); verify the question-on-home scenario and no-JS rendering (the hero-card grid and the journey rows were superseded by `redesign-minimal-shell`, whose home is a centred description, one search field, the topic counts, a link to `/tools/`, then recent and pinned. What survives is built and gated: the search is now a real form that works with JavaScript off and hands over to the palette when it is on; the featured tool renders its answer server-side, so the home shows a real computed result with no script; the four promises are in the description; browsing is one tap away.
  The question-on-home scenario runs through the same Wasm search module the palette loads and is pinned in `data/prefill-fixture.json` — "crosswind rwy 27 wind 300 at 15" fills runway 27, wind 300°, 15 kt. The scenario's "both marked assumed magnetic" does not match how the tool works: it needs only the angle between the wind and the runway, so it assumes no frame, warns that a runway number is a heading to within 5°, and its variation input says it is what mixes true and magnetic.)

## 3. Mobile and field

- [x] 3.1 Implement the numeric input contract with ± toggle and decimal-comma support, plus the attribute gate; verify both input scenarios
- [x] 3.2 Implement 48 px targets and Field mode (56 px, larger results, step buttons); verify the touch-target scenario
- [ ] 3.3 Implement the sticky answer bar tracking the visual viewport; verify the keyboard-open scenario on iOS and Android emulation (done so far: the bar lifts by what the visual viewport says the keyboard covers, and the page re-checks whether the answer card is still on screen each time it moves, both driven by a stand-in viewport in `apps/web/test/keyboard.test.mjs`; pending: the same scenario on real iOS and Android emulation, which waits on the browser matrix of task 3.5)
- [ ] 3.4 Implement responsive layouts (320 px up, side-by-side from 900 px and on landscape tablets, split-screen); verify the landscape-iPad scenario (done so far: the single column below 896 px and side by side above it, the phone layout across every split-screen width, and `apps/web/test/responsive.test.mjs` failing on any declared width, grid column, or page-level style too wide for a 320 px screen, with anything wide by design required to sit in a box that scrolls; pending: the landscape-iPad scenario itself, which needs the browser matrix of task 3.5)
- [x] 3.5 Build the mobile quality gates (full 320 px sweep, sampled landscape and 200% zoom, Chromium and WebKit, 2xx guard); verify the overflow scenario (built: `test/browser/reflow.test.mjs` sweeps every built page at 320 × 720 and a sample (every hub, the home page, 40 tools round-robin across domains) at 568 × 320 and at 375 px with 200% text, in Chromium and WebKit, each after a 2xx check, naming the route and the element that sticks out. First run found 14 pages whose prose tables scrolled the page at 320 px (explainers, quality, licenses) and 90 tool pages where the units picker and field grid outgrew a phone at 200% text; all fixed in CSS. It runs with the browser suite, not the per-commit gates)
- [x] 3.6 Wire one-tap sunlight and night modes, with the night canvas palette and no bright frames; verify the one-tap scenario and a luminance check (the separate `sunlight` and `night` modes were folded into paper and ink by `redesign-minimal-shell`, whose header carries exactly one display control and whose pre-paint script carries the retired names over; what this requirement asks of night is now checked against ink in `apps/web/test/theme.test.mjs`: every full-size surface below 0.05 luminance, map land no brighter than the page, the mode set before the stylesheet, and every status carried by a mark and words rather than color)
- [x] 3.7 Implement offline status chips and "Save this domain for offline"; verify the save-domain scenario (the service worker caches the whole release on install — every page, module, and data asset, 6.5 MB compressed against a 12 MB budget — so there is one pack rather than one per domain and a domain hub has nothing left to download; every page now carries a chip saying whether this browser holds the release: "Works offline", "Saving for offline", or "Needs the network", gated by `apps/web/test/offline.test.mjs`)
- [x] 3.8 Implement Web Share and the one-page mobile calculation sheet; verify the share scenario (the Share button opens the system share sheet with the sentence, its reference, and the permalink where the browser has one and copies the link where it does not; printing drops every control and navigation aid and leaves the tool, the inputs as typed, the answer with its sentence, and the terms, keeping the example chip so a sheet printed from an example still says so; gated by `apps/web/test/print.test.mjs`)

## 4. Usability validation

- [ ] 4.1 Recruit 5 testers per domain and run 5-second and task tests on hero tools; verify results recorded in `docs/usability/` and the 80% bars met, or pages revised and retested
