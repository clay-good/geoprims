# geoprims

**Geospatial and aerospace primitives.** Geodesy, navigation, aviation, drone, surveying, spatial-indexing, and terrain math, exact and cited, running entirely on your device.

- **For humans:** a fast static website at geoprims.com with a clean, minimal design and a live 2D map, 3D globe, and diagram canvas that draws, and where it helps animates, every result.
- **For agents:** a local MCP server that runs the exact same calculators. Clone a release tag and run `node mcp/server.mjs`, with zero dependencies and no network; `npx -y @geoprims/mcp` will also work once published. Setup: [mcp/README.md](mcp/README.md).

No ads, no accounts, no tracking, no server-side compute. Inputs never leave the device.

> **Status:** Phase 0 (platform) in progress. No tools are usable yet. The specs are in [OpenSpec](https://github.com/Fission-AI/OpenSpec) format, and the research behind them is in `docs/research/`. See [Progress](#progress) for what is built.

## What's specified

Planned inventory (targets from the specs; the live site will count only tools that have passed verification):

| Domain | Operations | Tool ids | Examples |
|---|---|---|---|
| Geodesy | 89 | 173 | DMS/MGRS/UTM/State Plane conversion, datums with epochs, geoid heights, WMM2025 declination |
| Navigation | 46 | 58 | Karney geodesics, rhumb lines, cross-track, fly-by turns, horizon and line of sight, 3D slant range |
| Geometry | 38 | 44 | Geodesic area, buffers, hulls, simplification, point-in-polygon, boolean operations |
| Aviation | 97 | 117 | ISA, CAS/TAS/Mach, density altitude, cold-temperature correction, E6B wind, W&B, METAR/TAF and winds-aloft decoding, holding entries, VDP |
| Drone | 49 | 49 | GSD, overlap and trigger timing, survey grids, endurance, VLOS guidance, lidar planning, link budget, dated Part 107/EASA references |
| Survey | 72 | 72 | Traverse closure, reductions, earthwork, curves, deed plotter, legacy land units, PLSS, GNSS field planning, ALTA precision |
| Indexing | 52 | 92 | H3 (full v4 API), S2, geohash, XYZ/TMS/quadkey tiles, Plus Codes, A5 |
| Raster | 30 | 40 | NDVI and other indices on local GeoTIFFs, elevation profiles, slope, terrain line of sight, viewshed |
| Time | 19 | 19 | Sun position, sunrise/sunset/twilight, the four legal "nights", Zulu, GPS week, Julian date |
| Units | 22 | 170 | Exact unit conversions (knots, nautical miles, inHg, US survey foot as legacy) |
| **Total** | **514** | **834** | |

An *operation* is a distinct calculation with its own test vectors. A *tool id* is anything you can open or call, including allow-listed conversion pairs built from operations (for example `dms-to-mgrs`). Search-friendly slugs such as `crosswind-calculator` are aliases and are not counted. About 300 tool pages are indexable. The rest open as presets of their parent tool, so search engines never see near-duplicates. Public counts include only tools that have passed the stable verification bar.

**Launch first, breadth second.** About 30 hero tools for pilots, drone operators, surveyors, and developers ship first, each reviewed, glance-tested, and mobile-gated ([launch plan](openspec/changes/plan-launch-and-value-proof/design.md)).

## Principles the specs enforce

- **One calculator, two surfaces.** A Rust → WebAssembly core. The website and the MCP server load byte-identical modules and return byte-identical results.
- **Reference accuracy.** Karney geodesics (not haversine by default), GeographicLib/PROJ/H3/S2/NOAA differential tests, and golden vectors from authoritative sources. A verification report is published with every release.
- **Explicit assumptions.** Datum realizations, epochs, height references, true vs magnetic, and rules of thumb vs exact values all surface as named warnings.
- **Honest time-sensitivity.** Magnetic models enforce their validity windows. NGS 2022 datums are labeled beta until adopted. FAA Part 108 is labeled "Proposed".
- **Private by construction.** Permalinks keep inputs in the URL fragment. The only network requests are same-origin static assets and coarse (≥ 1°) data tiles, plus a problem report if you choose to send one.
- **Glanceable.** Every tool opens with a real worked example and puts the answer first in a plain sentence ("Density altitude is 7,932 ft, about 2,900 ft higher than the field"), next to the rule of thumb and its error.
- **Show your work.** Every result has a "How we got this" panel with the formula using your numbers, cited sources with edition and section, assumptions, limitations, and a last-verified date. CI fails if a tool cites a superseded edition or an expired model.
- **Report a problem.** One button on every tool sends the exact inputs, outputs, and build to a small Cloudflare D1 inbox, after you preview the payload. There are no IPs, no accounts, and a public known-issues page.
- **Field-ready.** 48 px targets, numeric keypads that work, and one light/dark control in the header. It works offline and on a 320 px screen.
- **Accessible.** WCAG 2.2 AA in every theme. Audio is off by default. Reduced motion is respected.

## Build order

Each change in [`openspec/changes/`](openspec/changes/) has a proposal, a design, specs (except the planning change), and a task list.

| # | Change | Delivers |
|---|---|---|
| 1 | [`establish-platform-foundation`](openspec/changes/establish-platform-foundation/) | Tool contract, Wasm core, determinism, units, data assets, verification, privacy, catalog |
| 2 | [`define-build-contracts`](openspec/changes/define-build-contracts/) | Route map, page anatomy and notices, manifest fields, codes registry, reference profiles, report API, Cloudflare topology |
| 3 | [`build-web-experience`](openspec/changes/build-web-experience/) | Static site, command palette, map canvas (2D map, 3D globe, animation), themes, import/export, offline PWA, docs |
| 4 | [`add-trust-and-proof`](openspec/changes/add-trust-and-proof/) | Citations, standards freshness ledger, correctness program, proof panel |
| 5 | [`add-glanceable-and-field-ux`](openspec/changes/add-glanceable-and-field-ux/) | Answer-first anatomy, plain-language sentences, mobile input contract, field and night modes |
| 6 | [`add-problem-reporting`](openspec/changes/add-problem-reporting/) | Report button, Cloudflare Worker + D1, triage, known issues |
| 7 | [`add-seo-and-discoverability`](openspec/changes/add-seo-and-discoverability/) | Indexable page rules, structured data, OG images, sitemaps, llms.txt, natural-language prefill |
| 8 | [`add-local-mcp-server`](openspec/changes/add-local-mcp-server/) | Zero-dependency local MCP server, clone-and-run, six meta-tools |
| 9 | [`add-geodesy-suite`](openspec/changes/add-geodesy-suite/) | Parsing, frames, datums, projections, grid references, heights, geomagnetism |
| 10 | [`add-navigation-and-geometry`](openspec/changes/add-navigation-and-geometry/) | Geodesics, routes, line of sight, 3D vectors, computational geometry |
| 11 | [`add-aviation-suite`](openspec/changes/add-aviation-suite/) | Atmosphere, airspeed, altimetry, wind/E6B, performance, fuel and loading |
| 12 | [`add-drone-suite`](openspec/changes/add-drone-suite/) | Photogrammetry, mission patterns, endurance, operations references |
| 13 | [`add-survey-suite`](openspec/changes/add-survey-suite/) | COGO and traverse, reductions, earthwork, alignment curves |
| 14 | [`add-spatial-indexing-and-raster`](openspec/changes/add-spatial-indexing-and-raster/) | H3/S2/geohash/tiles/Plus Codes, spectral indices, terrain analysis |
| 15 | [`add-practitioner-essentials`](openspec/changes/add-practitioner-essentials/) | Sun and time, weather decoding, IFR geometry, deeds and PLSS, GNSS field tools, drone sensors and links |
| 16 | [`plan-launch-and-value-proof`](openspec/changes/plan-launch-and-value-proof/) | Phases, hero tools, launch bar, tracking-free value metrics, cut list |

Changes 1–8 are the platform (Phase 0), with `define-build-contracts` authoritative wherever two specs meet. Domain changes 9–14 can proceed in parallel once geodesy (9) lands, because the others reuse its parsing, frames, and heights. Practitioner essentials (15) comes last because it builds on every suite. Change 16 sets the order of work: hero tools first.

```bash
openspec list
```

```bash
openspec validate --all --strict
```

## Progress

| Change | Built so far |
|---|---|
| `establish-platform-foundation` | Monorepo layout and pinned toolchain; one import-free `.wasm` per domain behind a raw C ABI, with an import lint and Brotli budgets, each proven against a bad fixture; `gp-base` (exact unit registry, unit-tagged parsing, angle normalization, error and warning model, ECMAScript-format numbers, unit profiles, result envelope); the tool runtime (`invoke`, `invokeBatch`, `manifest`) and manifest lint; `packages/runtime` (shared loader, input hardening, Node host); `catalog/v1.json` generation; data assets (`assets/registry.json`, the core's `ASSET_UNAVAILABLE` request, and Node and browser providers that check SHA-256 before supplying a file); golden vectors with a provenance lint and a silent-edit guard; **the units domain: 22 operations and 35 allow-listed pair endpoints (57 tool ids), all experimental, with 230 golden vectors** |
| `add-local-mcp-server` | Zero-dependency stdio server with all six meta-tools: `geoprims_search` (core ranker in its own `search` module), `describe`, `run`, `pipeline`, `convert_units`, and `report_problem`; catalog and tool resources; per-call timeouts in a worker thread; a golden surface file; every golden vector checked byte for byte against the runtime; operating caveats (`meta.notice`, `meta.context`) on operational results; opt-in direct toolsets (`--toolsets`, `--no-meta`) of stable tools; five workflow prompts; paged long lists (`output.maxItems`, `offset`); setup snippets for five clients shared by the README and the site's [Use with agents](https://geoprims.com/agents/) page, each launched by a smoke test; request-size, trap, and log-privacy tests; a network audit that runs every tool's worked example inside an OS sandbox with no network (sandbox-exec on macOS, an empty network namespace on Linux) and a hook that fails on any connection attempt; 31 end-to-end tests ([mcp/README.md](mcp/README.md)) |
| `add-drone-suite` | Mapping photogrammetry: GSD across and along track (catches 35 mm-equivalent focal lengths), height for a target GSD with an altitude-ceiling check, footprint, trigger interval, and line spacing with overlap presets and a camera-too-slow check, motion blur with the slowest shutter, and ASPRS Edition 2 accuracy with checkpoint error and the 30-checkpoint minimum. Power: battery energy (mAh to Wh) with usable energy and C-rate, momentum-theory hover power, flight time with reserve and a labeled cold-battery derating, maximum payload, and the return-to-home budget. Operations: the Part 107 altitude limit with the structure exception, groundspeed and kinetic-energy checks, the EASA open subcategory, and EASA VLOS guidance, all read from dated reference data (`data/regulations.json`) with a not-legal-advice notice. Missions: a survey grid (fewest lines by default, crosshatch, overshoot, no-fly holes routed around), image count from the same sweep, corridor lines that follow a centerline, and orbits with heading and gimbal pitch |
| `add-geodesy-suite` | The surveyor converter set: a coordinate parser (DD, DMS, DDM, Unicode marks, hemisphere letters, packed aviation, labeled pairs, MGRS, UTM) that reports ambiguity instead of guessing, a formatter that carries rounding and reports ground resolution, bearing difference, UTM forward and inverse (Karney's 6th-order Krüger series, the Norway and Svalbard exceptions, forced zones, convergence and scale), UPS, zone lookup, and MGRS encode and decode (truncation, polar bands, band tolerance, invalid squares). Every latitude and longitude field in the catalog now accepts DMS. Magnetic declination and the full field from WMM2025 (matching all 100 NCEI test values) or IGRF-14 back to 1900 (matching ppigrf within 0.001 nT), with validity windows, the WMM uncertainty, compass blackout and caution zones, and true ↔ magnetic bearings from a chart variation, the model, or both. State plane: SPCS83 forward and inverse in all 124 zones (meters, international feet, or US survey feet) with convergence and scale factor, matching PROJ within 0.05 µm, plus zone lookup. Heights: EGM96 geoid height and ellipsoidal ↔ orthometric conversion, matching GeographicLib GeoidEval, with the grid loaded as an on-demand, digest-checked data asset; ellipsoids and frames: parameters for seven catalog ellipsoids or a custom one (b, eccentricities, third flattening, mean, authalic, and volumetric radii), radii of curvature and degree lengths with exact meridian arcs (Carlson's elliptic integrals, within 1 nm of GeographicLib), all six auxiliary latitudes both ways (within 1e-13° of 40-digit values), geodetic ↔ ECEF by Vermeille's closed form (matches CartConvert; round trips within 5 nm), and local ENU/NED/AER both ways with the rotation matrix and a sky plot; grid references beyond MGRS: USNG (spaced, and truncated local references with a grid zone), Maidenhead locators from 2 to 10 characters, GARS, and GEOREF, each drawn as its cell on the map, with GARS and GEOREF identical to GeographicLib's C++ classes on 6,496 cases; datum transformations: a 7- or 14-parameter Helmert tool with an explicit position-vector or coordinate-frame convention and an exact reverse (matches both IOGP Guidance Note 7-2 worked examples and PROJ), and ITRF2020 back to ITRF88 plus the WGS 84 realizations aligned with them, using the IERS parameters at the coordinates' epoch; plate motion between epochs with the ITRF2020 plate motion model (13 plates, origin rate bias) or a site velocity, flagging Bird's PB2002 deformation zones such as the San Andreas system; NAD 83 (2011, PA11, MA11) to and from ITRF and WGS 84 with the NGS HTDP parameters, matching HTDP 3.6.0 itself within 1 mm; and legacy datums (ED50, NAD27, OSGB36, Tokyo, AGD66, Pulkovo 1942, SAD69, Arc 1960) to and from WGS 84 with their EPSG operations, always stating the meter-level accuracy and flagging points outside each transformation's area of use; each region's old datum ↔ NAD 83 with the NGS NADCON5 grids (NAD 27 in the conterminous US and Alaska, Old Hawaiian, Puerto Rico 1940, St. Paul 1952, American Samoa 1962, Guam 1963; a 1.8 MB on-demand asset, biquadratic interpolation, matching PROJ to 1e-9°) |
| `add-aviation-suite` | Slice 1 of the pilot hero tools: ICAO and US 1976 standard atmosphere (to 86 km, closed-form inverses), pressure altitude (METAR `A2980`/`Q1009` groups), density altitude (dry or humid, with both rules of thumb and their errors), ISA temperature, runway crosswind and headwind (designators, gusts, variable winds, personal limits with Within/Near/Beyond phrases), and the wind triangle (heading and groundspeed, find the wind), with true/magnetic mixing rejected unless a variation is given. Slice 2: CAS, TAS, Mach, and EAS by the exact compressible-flow path (Rayleigh pitot above Mach 1, POH calibration tables for IAS, and the 2% rule with its error), total and static air temperature, turn performance with load limits, top of descent with the 3-to-1 and 5 × groundspeed rules, climb gradient, visual descent point, glide range with wind, pivotal altitude, fuel weight, and weight and balance with a CG envelope check at takeoff and landing. Slice 3: a METAR/SPECI decoder (US remarks, flight category, trend groups kept apart from the observation, and every unread group listed), a TAF decoder with a UTC and local timeline of FM, TEMPO, BECMG, and PROB periods (TX/TN temperatures decoded; groups outside the valid period flagged), an FB winds-aloft decoder (light and variable, 100+ kt, implied minus signs; paste a whole product and its FT header sets the levels), holding entry per AIM 5-3-8, holding wind correction and timing, and maximum holding speeds |
| `add-navigation-and-geometry` | Geodesics on any ellipsoid: Karney inverse (with m12, M12, M21, S12) and direct via `geographiclib-rs`, midpoint, haversine with its error against the geodesic, and legacy Vincenty inverse and direct that fail honestly near antipodes and report their difference from Karney; a 2,000-pair Karney–Vincenty differential agrees within 1 mm; rhumb lines (loxodromes) inverse and direct on the ellipsoid, within 1 µm of GeographicLib's RhumbSolve on 2,000 pairs, reporting the extra distance over the geodesic and stopping at a pole; cross-track and along-track distance to the millimeter on the ellipsoid (or the classic spherical formula); fly-by turn anticipation, time-speed-distance with ETAs, closest point of approach, and route legs with true and magnetic courses, leg times, and arrival times; line of sight: horizon distance, mutual visibility and hidden height, dip, and Fresnel clearance; waypoints along a line (with GPX and GeoJSON) and the closest point on a multi-leg route; the first geometry tool, polygon area and perimeter on the ellipsoid with holes, across the antimeridian and around the poles, within 3e-9 of GeographicLib's Planimeter on 500 polygons |
| `add-practitioner-essentials` | Time scales, with no host clock or time-zone data: UTC to GPS week and seconds (10-bit weeks need their rollover era), GPS to UTC through leap seconds (23:59:60 round-trips), TAI − UTC from IERS Bulletin C 72 (echoed in `meta.assets`, with a warning after the table expires), Julian date, MJD, and day of year with the RINEX daily file name, decimal hours (1.3 h is 1:18), block time across midnight, local to Zulu with IANA time zones (tzdb 2026d embedded, matching Python zoneinfo on 238,800 checks; DST gaps and overlaps are flagged; legacy names such as CET and EST5EDT accepted), and zone rules with the next change. Sun tools: position by the NREL Solar Position Algorithm (the NREL worked example to 1e-5°, pvlib to 1e-8°) with the NOAA result as a cross-check, ΔT and DUT1 inputs, shadow length and slope incidence; sunrise, sunset, and all three twilights from the NREL SPA's geometric sun (1,200 events at 300 places and dates within a minute of USNO, including a twilight that grazes −6° by 0.013°), with polar-day and polar-night states, each dated in local time and Zulu; the four US aviation nights (§1.1, §61.57(b), §91.209, §107.29) with a landing check; and the mapping light window |
| `add-spatial-indexing-and-raster` | H3 (on h3o): point to cell, a cell inspector with boundary, disks, rings, paths and grid distance, parent and children, compact and uncompact, edges and vertexes, a resolution chooser, and polygon fill in center, full, and overlapping modes. Indexes match H3 C on 1.6 million comparisons, and fills match H3 C on 1,000 random polygons. Also: geohash encode, decode with error margins, and neighbors (wrapping across ±180°, none past a pole); web map tiles (point to XYZ, TMS, and quadkey; tile or quadkey to bounds, with a detect mode for the y-convention mix-up; ground resolution and scale); and Plus Codes (encode, decode, shorten, and short-code recovery with a reference point, passing every case in the official Open Location Code test files) |
| `add-survey-suite` | Slice 1 of the surveyor tools: bearing and azimuth parsing (`N 45°30'15" E`, `S44-30-00W`, or 224.5), inverse and forward, traverse closure with the compass or transit rule (misclosure, direction, `1:N` precision, the adjusted loop), area by coordinates in acres and hectares, circular curves from any two elements (arc or chord degree of curve; a tangent and middle ordinate that fit two curves are flagged) with PC and PT stations, vertical curves with the high or low point and K, average end area and prismoidal volumes, swell and truck loads, and the combined grid-to-ground factor. Mixing US survey feet and international feet is refused. List inputs (traverse courses, parcel corners) edit as one row per line on the web and survive permalinks. Land descriptions: a metes-and-bounds deed parser (bearings in symbols or words, chains and links, varas, curves, monuments kept as text), deed plotting with misclosure, precision, and area by an implied closing line (never adjusted unless you choose), in the calls' own unit, legacy land units (chains, rods, varas and arpents by jurisdiction), a PLSS aliquot reader with nominal area, and basis-of-bearing rotation |
| `build-web-experience` | Astro static site ([apps/web](apps/web/README.md)): home, domain and group hubs, and one page per tool id with the worked-example answer pre-rendered in the HTML; a Svelte island with a schema-driven form, live recompute in a Web Worker, core-rendered answers, sentences, comparison lines, and status phrases, severity-ordered warnings, copy actions, and `#v1:` permalinks; an installable PWA whose service worker precaches the whole release (4.3 MB compressed) so every tool works offline after the first visit, with updates that wait for "Reload to update"; a command palette (`/` or Ctrl/Cmd+K) that ranks with the same core search as `geoprims_search`, 1.6 ms p95 at 1,000 entries, that also recognizes a pasted H3 cell, geohash, tile, Plus Code, coordinate, MGRS reference, METAR, or altimeter group and opens the right tools pre-filled; the contract's CSP (inline bootstrap allowed only by build-computed hash) and security headers in `_headers` and a meta tag, with a smoke test; the Atlas design: two display modes (paper, the light default, and ink) with one signal-orange accent and self-hosted Geist fonts (52 KB), chosen before first paint and each tested for AA contrast; a map canvas (2D map or globe) that draws each geographic tool's lines, points, and polygons in true geometry over Natural Earth 110m, offline; and vector diagrams for the wind triangle, runway wind components, closest point of approach, and fly-by turns; settings for unit profile and number format that apply to every tool, recent and pinned tools, and keyboard shortcuts with a `?` sheet; a minimal shell: a header carrying only the site title and the light/dark control, a home page that explains the product and leads with one search field and the topic categories, a catalog page listing every tool by category, tool pages with values and answer side by side (answer first on phones, with a bottom answer bar while you edit), results as a label/value list, rarely used inputs under More options, readable units (°C, degrees), a shaded globe with labeled markers, a line legend, a scale bar, and zoom buttons; single points framed with about 1,000 km of context, and a base map with state and province lines and city labels that thin out by zoom (Natural Earth 1:110m, 76 KB) |
| `define-build-contracts` | Permalink fragment codec in the core (`link` module: deterministic DEFLATE, base64url, the fragment grammar) with a pinned vector file that Node's zlib cross-checks; `data/codes.json`; the shared report limits; the "Wrong answer" issue form; a glossary (`data/glossary.json`) of every abbreviation the tools use, each with a plain definition and a cited source, gated so a new label cannot introduce an undefined abbreviation, and shown on each tool page under "Terms on this page"; a manifest meta-schema gate that allows only the contract's extension fields and at most five core inputs per tool; and a route-map gate in the web build that classifies all 289 built pages against the route map, fails naming any page outside it, holds each class to its canonical or `noindex` rule, and writes `_redirects` from `data/redirects.json` so a renamed tool's old route keeps resolving |
| `add-glanceable-and-field-ux` | The sentence-template language rendered in the core (fields, forced units, `delta`/`abs`, `if`/`else`, `warn`, `plural`), deterministic display rounding and digit grouping, a template lint, and a grade-8 readability lint; every result carries its `summary` sentence |
| `add-problem-reporting` | The report loop, ready to deploy: a "Report a problem" button on every tool that lazy-loads a dialog showing exactly what will be sent (with an include-inputs toggle, a 280-character note, and paused and offline states with Copy report); the Cloudflare Worker ([worker/](worker/README.md)) with strict validation, a uniform 202, keyed daily reporter hashes kept only in counters, attempt and accepted caps, dedupe, Turnstile checks, the daily cleanup, logging off, and a kill switch; the D1 migration generated from the shared limits; and a `/known-issues` page with tool banners. The dialog's payload passes the Worker's validator for every tool. Not deployed yet: it needs the D1 database, secrets, and the WAF rule |
| `add-seo-and-discoverability` | Build outputs: `/llms.txt`, `/AGENTS.md`, `/.well-known/mcp.json` (from the MCP golden surface), `robots.txt`, and per-domain sitemaps with a content-hash lastmod ledger; allowlisted JSON-LD (WebApplication, CollectionPage, BreadcrumbList) with a gate; and a "For developers and agents" block on every tool whose `geoprims_run` call is tested to reproduce the page; natural-language prefill: one parser in the core search module pulls quantities out of a question (`5000 ft`, `30C`, `A2992`, `rwy 27`, `300@15`, `30015G25KT`, `13.2mm`, `40.44, -79.98`), ranks on the remaining words, and fills the top tool's inputs through per-tool slots checked by the manifest lint, filling only what is unambiguous. The palette offers "Open with these values" or one choice per placement of ambiguous values, and `geoprims_search` returns the same `prefill`, checked by a 19-question fixture and an accuracy gate over 557 questions generated from every worked example (top-1 94.8%, top-3 99.8%, prefill precision 100%, recall 44.6%; CI fails on a drop of more than 2 points and lists the questions that changed) |
| `add-trust-and-proof` | The sources ledger (`data/sources-ledger.json`, 74 rows) with build gates for coverage, superseded editions, overdue checks, model expiry, and forward-only stamps; `/sources` (edition, last check, and the tools citing each source) and `/methodology` (what is checked and what is not yet); per-tool test-vector downloads; a bounds fuzzer over every tool (in CI at 40 cases per tool; 1,000 per tool passes) whose findings are fixed and pinned as regressions; a dimension lint over every input and output; the stable-promotion gate (derivation note, independent worked example, invariant and differential tests, 20+ vectors, search reachability), which seventy-five stable tools pass: magnetic declination, SPCS83 forward and inverse, EGM96 geoid height, H3 cell for a point, sun position, Karney geodesic inverse and direct (checked on 1,000 lines of GeographicLib's GeodTest), UTM forward and inverse (1,000 exact transverse Mercator points from TMcoords), the standard atmosphere (against the separately written `ambiance`), and four pilot hero tools, pressure altitude, density altitude, runway crosswind and headwind, and the wind triangle, each matched to the FAA Pilot's Handbook (FAA-H-8083-25C) worked examples and 20+ independent vectors; and five developer hero tools, geohash, map tile for a point, tile bounds, H3 grid disk, and haversine, each checked on 250 to 1,000 cases against a separately written library (pygeohash, mercantile, H3 C, GeographicLib) through the public tool interface; the drone GSD and height-for-GSD tools, matched to a published worked example (arXiv:2108.12811) with 20+ vectors each; and the surveyor tools traverse closure (the University of Memphis worked traverse, including the compass-rule balanced table) and the combined grid-to-ground factor (all five lines of the NOAA Manual NOS NGS 5 example); and MGRS encode and decode (the FGDC USNG standard's Washington Monument example, 2,000 references identical to NGA GEOTRANS, with PROJ settling the cases where GEOTRANS's own projection is 1.5 cm off); and weight and balance (the loaded light twin, weighing, and percent-MAC examples in the FAA Weight and Balance Handbook, FAA-H-8083-1B); sunrise, sunset, and twilight (1,200 events within a minute of the US Naval Observatory); the four aviation nights (491 regulatory windows built from USNO times at 100 places); local time to Zulu (2,388 instants identical to Python zoneinfo on the same tzdb); and the horizontal curve (FM 5-233's arc- and chord-definition examples), vertical curve (the Indiana Design Manual example, whose printed low-point elevation has an arithmetic slip the core does not repeat), area by coordinates; and top of descent and the visual descent point (the Instrument Procedures Handbook's 3-to-1 and 300 ft per NM examples); and the METAR decoder (612 live US and European reports against python-metar, plus the Aviation Weather Handbook's decoded KOKC report) and winds aloft decoder (the handbook's Kansas City product, pasted whole); and the TAF decoder (588 live US, Canadian, and European TAFs against pytaf, and the handbook's Pierre, SD example); battery energy, mAh to Wh (the FAA PackSafe example); and the coordinate reader (1,500 independently written strings in ten notations, which found and fixed two misreads and a crash); the deed plotter (the Memphis worked traverse as a deed, closure and area); EASA VLOS guidance (the German LBA's worked table, now capped at 1,500 m as the guidance intends); and the rest of the H3 family, cell inspector, ring, path, parent, children, compact, uncompact, and edges (300 cells with pentagons identical to H3 C through the public tools); and the rest of the indexing tools, geohash decode and neighbors (pygeohash), Plus Code encode, decode, and shorten (every row of the Open Location Code test data, which found an 8-digit code shortened to a bare "+"), tile ground resolution (the Bing Maps table), the H3 resolution chooser (the H3 resolution table), and H3 polygon fill (H3 C's San Francisco test polygon); and the four frame conversions, geodetic to ECEF and back, and to and from a local east-north-up frame (the IOGP Guidance Note 7-2 North Sea examples, and 1,500 cases against GeographicLib's CartConvert within 15 nm); and the grid references GARS, GEOREF, Maidenhead, and USNG, each way (NGA's GARS bands, Wikipedia's GEOREF and Maidenhead examples, and the FGDC USNG Washington Monument; GeographicLib's C++ classes and pygeodesy for GARS and GEOREF, an exactness-checked Maidenhead reference, which found typed grid-line values landing on either side of the line, and the GEOTRANS MGRS fixture for USNG); and the rhumb line both ways, matched to Bowditch's Mercator sailing examples (NGA Pub. 9, 2024) and to GeographicLib's RhumbSolve on 2,000 pairs through the tools); and cross-track distance, matched to Ed Williams' Aviation Formulary example and to a brute-force search along the geodesic); and time, speed, and distance, checked against every one of the 4,800 cells of Bowditch's printed Table 11, which turned up a misprint the core does not repeat); and distance to the horizon, against all 126 heights of Table 12, which turned up a second one); a per-hero-tool launch checklist ([docs/launch/hero-tools.md](docs/launch/hero-tools.md)); practitioner sign-off records ([docs/review-signoffs.md](docs/review-signoffs.md)) with "not yet reviewed" disclosures on every domain and tool page; a claims gate that checks public statements against the build; a per-release verification report (`/verification/<version>/`, every vector rerun, the worst error per tool against its tolerance); and a changelog that must label every superseded vector as a result change |
| `redesign-minimal-shell` | The minimal shell: a header carrying only the site title, a one-line description, and a light/dark control; the utility links moved to the footer; two display modes (paper, the light default, and ink) in place of five, with the retired names mapped to their replacement; a home page that explains the product in three sentences, then one search field, then the topic categories with their counts; a catalog page at `/tools/` listing every operation by category with an `ItemList` listing; hairline-separated tool rows, radii capped at 8 px, tabular figures, and a graticule motif, so the shell stays as spare as its sister site roughlogic.com while looking like itself |
| Everything else | Not started |

## Building it

Requirements: [rustup](https://rustup.rs) (it installs the pinned toolchain from `rust-toolchain.toml`, including the wasm target), binaryen's `wasm-opt` at the version in `tools/toolchain.json`, and Node 22 or newer. The core, runtime, and MCP server have no npm dependencies; only the website (`apps/web`) installs build tools (Astro and Svelte).

```bash
cargo test --manifest-path core/Cargo.toml
```

```bash
npm run build
```

```bash
node mcp/server.mjs
```

```bash
npm run test:js
```

The build writes `dist/wasm/<module>.wasm` and `dist/wasm/modules.json` (sizes and SHA-256 digests), and the catalog step writes `dist/catalog/v1.json`. It fails if the toolchain versions differ from the pins, if any module imports anything from the host, or if a module exceeds its compressed budget (base 120 KB, domain 400 KB).

[AGENTS.md](AGENTS.md) holds the working rules for anyone, human or AI, implementing the specs: the three doors (web, MCP, report), how to add a tool, and the non-negotiables.

## Research

The specs cite seven research briefs gathered on September 18, 2026. Items marked unverified still need a primary-source check during implementation.

- [Geodesy libraries, datums, and reference data](docs/research/01-geodesy-and-reference-data.md)
- [Spatial indexing, Wasm toolchain, rendering, and hosting](docs/research/02-indexing-wasm-rendering-hosting.md)
- [Aviation, drone, surveying, and remote-sensing formulas](docs/research/03-aviation-drone-survey-formulas.md)
- [MCP, competitors, legal, and accessibility](docs/research/04-mcp-competitors-legal-a11y.md)
- [Feedback backend, SEO, mobile and field UX, trust patterns, MCP distribution](docs/research/05-feedback-seo-ux-trust-mcp-distribution.md)
- [Patterns reused from roughlogic.com](docs/research/06-roughlogic-patterns.md)
- [Practitioner gap analysis, hero tools, and journeys](docs/research/07-practitioner-gap-analysis.md)

## Disclaimer

geoprims tools are planning, engineering, and education aids. They are not certified for navigation and are not legal survey determinations. Verify operational values against official sources, your aircraft's POH/AFM, and current regulations.

## License

[MIT](LICENSE)
