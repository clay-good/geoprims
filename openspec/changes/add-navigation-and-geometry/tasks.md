## 1. Geodesics

- [x] 1.1 Wrap `geographiclib-rs` inverse and direct with the tool contract (including m12, M12, S12); verify the JFK-LHR, nearly-antipodal, antipodal, coincident, and direct scenarios and the GeodTest short set (690 KB) in CI (a 1,000-line sample of GeodTest-short runs in CI: distances within 15 nm, observed 7.5 nm; both tools are stable)
- [x] 1.2 Implement Vincenty direct and inverse with iteration limits and Karney delta; verify the non-convergence and comparison scenarios
- [x] 1.3 Implement spherical methods with radius handling and ellipsoidal delta; verify the haversine-error scenario (haversine with the error scenario; `navigation.geodesic.spherical-inverse` (distance, initial and final course, and the distance and course difference from WGS 84), `navigation.geodesic.spherical-direct` (destination and its offset from the ellipsoidal one), and `navigation.geodesic.intermediate-point` (slerp at a fraction beside the same fraction of the geodesic, refused for antipodal points), all on R1 by default; 16 golden vectors with the ellipsoidal parts from GeographicLib's GeodSolve)
- [x] 1.4 Port GeographicLib Rhumb (direct, inverse, pole handling); verify the rhumb scenarios and a `RhumbSolve` differential test (gp-geo rhumb.rs with divided differences for nearly east-west rhumbs; 2,000 RhumbSolve pairs agree to 0.4 µm and 2e-11° inverse and 47 nm direct, 14 µm for starts within 0.01° of a pole; JFK-LHR and stop-at-the-pole scenarios)
- [x] 1.5 Implement waypoints, densification, midpoint, and intermediate points with GPX/GeoJSON output; verify the equal-intervals scenario (navigation.geodesic.waypoints: N intervals, a spacing, or fractions along a geodesic or rhumb line, with distance and course at each point, as a table, a GPX route, and GeoJSON; the 11-point JFK-LHR scenario; 22 vectors from Python geographiclib)
- [x] 1.6 Port GeographicLib Intersect and implement the vertex tool; verify the non-intersecting scenario and an `IntersectTool` differential test (`navigation.geodesic.intersection`: the crossing nearest the segments' midpoints, as in Karney's Intersect, flagged when it lies on an extension; 60 random pairs and the non-intersecting scenario match IntersectTool -i. `navigation.geodesic.vertex`: the northern vertex by Clairaut's relation and the direct problem in arc length, checked against GeodSolve -a and the closed-form vertex latitude. Along the way, the shared Newton solver no longer gives up when rounding holds the gap just above a nanometer)
- [x] 1.7 Support custom ellipsoids with GeodesicExact above |f| > 0.02; verify the Mars and high-flattening scenarios (custom ellipsoids with the Mars scenario; for 0.02 < |f| ≤ 0.5 the inverse and direct tools use `gp_geo::exact`: the integrals behind GeodesicExact by Gauss-Legendre quadrature, with the inverse bisected on the start azimuth in Karney's canonical position. `meta.model` names GeodesicExact, and 480 geodesics at six flattenings match GeodSolve -E to 3e-15 in distance and 1e-13° in azimuth, with the reduced length, scales, and area. Other geodesic tools still refuse |f| > 0.02, and nearly antipodal points are refused on these ellipsoids)
- [ ] 1.8 Implement the comparison overlay (geodesic, rhumb, great circle); verify the visual fixture

## 2. Route geometry

- [x] 2.1 Implement ellipsoidal cross-track/along-track with foot-point iteration and the spherical mode; verify both cross-track scenarios and 1 mm agreement with a brute-force reference (navigation.route.cross-track by Karney's gnomonic interception method; 200 random cases within 1 mm of a perpendicularity search along the geodesic; 22 vectors from Karney's Python geographiclib; right-of-course and past-the-end scenarios; rhumb segments pending)
- [x] 2.2 Implement closest point on multi-leg routes; verify the leg-identification scenario (navigation.route.closest-point: leg, along-route distance, signed cross-track, clamped at leg ends; 22 vectors from Python geographiclib)
- [x] 2.3 Implement course intersection and intercept; verify the unreachable-target scenario (`navigation.route.course-intersection`: geodesic courses by Newton's method on the two distances from the great-circle seed, rhumb courses exactly in Mercator coordinates, the crossing ahead of both preferred and runs signed; `navigation.route.intercept`: the first time the range to a steadily moving target equals your run, by steps that cannot pass it and a bracketing finish, with the spec's unreachable message when the target opens faster than you close. 13 golden vectors from GeographicLib's C++ IntersectTool, GeodSolve, and RhumbSolve, matching to the micrometer)
- [x] 2.4 Implement fly-by turn anticipation and standard-rate turns; verify the 90° and standard-rate scenarios (navigation.route.fly-by: radius, lead distance, arc, time, and FLY_OVER_RECOMMENDED above 120°; standard rate by default; 833.4 m and 18.24° scenarios; 22 vectors)
- [x] 2.5 Implement geodesic range rings with pole and antimeridian handling; verify the pole-enclosing scenario with a GeoJSON validator (`navigation.route.range-rings`: up to 20 radii around a center, each ring by the geodesic direct problem at equal azimuth steps, counterclockwise, densified so each chord sags under 0.025% of the radius or 0.125 m; areas by Karney's polygon area; a downloadable GeoJSON that splits a ring crossing the antimeridian into a MultiPolygon and runs a ring around a pole along ±180° to it. A Rust validator checks closed rings, positions in range, counterclockwise exteriors, and no edge across ±180° for the 1,500 km ring at 85° N (enclosing the pole), a ring at 89° S, a ring across the antimeridian, and an ordinary ring; 11 golden vectors with areas from GeographicLib's Planimeter within 1e-9)
- [x] 2.6 Implement multi-leg route totals with magnetic courses, times, and ETAs; verify the magnetic-course scenario (navigation.route.legs: per-leg distance, initial and final true course, WMM2025 declination and magnetic course at each leg's start, cumulative and total distance, leg times and arrival times; geodesic or rhumb legs; 22 vectors from Python geographiclib)
- [x] 2.7 Implement time-speed-distance and 2D CPA; verify the solve-for-time and CPA scenarios (navigation.route.time-speed-distance solves any one of three with ETA from a clock time and explicit UTC offset; navigation.route.cpa in a local plane with DIVERGING; the 2 h 00 min and 110 s / 141.42 m scenarios; 22 vectors each)
- [ ] 2.8 Implement route visualization (legs, turn arcs, offsets, rings); verify the turn-arc fixture (done so far: the turn-arc fixture — the fly-by drawing's legs run along the courses through the waypoint, the arc leaves the inbound leg and rejoins the outbound one the same distance either side, sweeps the way the core says, and its lead over radius is the tangent of half the turn, as the core has it. Routes draw their legs: `navigation.route.legs` returns the waypoints in order and the map draws them as one line with direction arrows and a Start marker, and no longer lays the same waypoints down twice as input and result. Range rings already draw as polygons. Pending: offset tracks)

## 3. Line of sight and 3D

- [x] 3.1 Implement horizon models, mutual visibility, hidden height, dip, and geographic range; verify the 100 m, hidden-height, and dip scenarios (navigation.los.horizon, .visibility, .dip with geometric, optical, and radio models and the rules of thumb with their errors; every result carries TERRAIN_NOT_CONSIDERED; 22 vectors each)
- [x] 3.2 Implement Fresnel zone and Earth-bulge clearance; verify the 5.8 GHz scenario against the textbook formula (navigation.los.fresnel: first Fresnel radius, 60% clearance, and Earth bulge at any point, K = 4/3 by default; cites ITU-R P.530-19, verified at the issuer)
- [x] 3.3 Implement 3D distance with height-reference reconciliation and look angles; verify the drone and below-horizon scenarios (`navigation.vector.distance-3d`: straight-line distance through ECEF with the ground distance, elevation angle, and the flat-Earth figures beside them; sea-level heights become ellipsoidal through EGM96, and mixed references without a geoid are refused. `navigation.vector.look-angles`: azimuth, elevation, and range with optional refraction and `BELOW_HORIZON`. The drone scenario gives 2,003.694 m and 3.4245°; 13 vectors agree with GeographicLib CartConvert, GeodSolve, and GeoidEval to 1e-6 m)
- [x] 3.4 Implement vector algebra with convention selection and 3D CPA; verify the navigational-convention and vertical-separation scenarios (`navigation.vector.polar-cartesian` converts both ways with the navigational or mathematical convention stated, and 10 at 090 gives (10, 0); `navigation.vector.operations` sums any number of 2D or 3D vectors and, for two, gives the difference, dot and cross products, angle, and projection. The 3D CPA is the existing `navigation.route.cpa` with optional height and climb-rate inputs: head-on traffic 1,000 ft apart reports 0 horizontal and 1,000 ft vertical)
- [ ] 3.5 Implement vector-diagram rendering (head-to-tail, rotatable ENU); verify the visual fixture (done so far: the head-to-tail drawing for `navigation.vector.operations` — each vector starting where the last ended in the east-north plane, the resultant from the origin labeled with its magnitude and direction, and any vertical component written into the label rather than faked on a plan view. Its fixture checks the arrows carry the components to one scale, that each starts where the last ended, and that the resultant runs origin to last head at the direction the core computed. Pending: rotating the ENU frame, which needs a control on the canvas rather than a static drawing)

## 4. Computational geometry

- [x] 4.1 Implement geodesic and planar area/perimeter with orientation and pole handling; verify the Colorado, polar-cap, and shoelace-warning scenarios and a `Planimeter` differential test (built: geometry.area.polygon with geodesic edges, holes, orientation, antimeridian and pole handling; 500 Planimeter polygons agree within 3e-9 relative; Colorado and polar-cap scenarios.; rhumb edges by Karney (2024), matching Planimeter -R within 1e-8 on 5 polygons including an antimeridian crossing and a polar cap; and a planar shoelace mode that warns `PLANAR_ON_GEOGRAPHIC` with its error against the geodesic area and points to the geodesic mode)
- [x] 4.2 Implement centroids and representative point; verify the C-shape scenario (`geometry.shape.centroid`: geodesic edges cut into 5 km pieces and mapped to a Lambert azimuthal equal-area plane on WGS 84's authalic sphere, an exact equal-area map (authalic latitude inverted by Newton's method); the area-weighted centroid with holes subtracted, mapped back; CENTROID_OUTSIDE when it falls outside; and the pole of inaccessibility by polylabel on the same plane, with its clearance measured as a geodesic distance. The C-shape flags and puts the interior point in the solid bar; 13 golden vectors from symmetry, local-plane triangle centroids, a centered hole, and a ring around the pole. The planar centroid for projected input is pending)
- [ ] 4.3 Implement hulls, antimeridian-aware bbox, MBR, and minimum enclosing circle; verify the antimeridian scenario (done so far: `geometry.shape.bbox` for points, lines, and polygons: longitudes as the complement of the largest uncovered gap, each geodesic edge covering its shorter arc; latitudes including each edge's vertex, found by bisection where its azimuth passes 90° or 270°; a polygon circling a pole spans every longitude and reaches it. The antimeridian scenario gives west 170, east -170, a 20° span, with CROSSES_ANTIMERIDIAN; 13 golden vectors, the edge vertices from Clairaut's relation with GeodSolve azimuths. Also `geometry.shape.enclosing`: the smallest enclosing circle by Welzl's algorithm on an azimuthal equidistant plane re-centered on its result until the center is the plane's own, which is the exact geodesic optimum since that plane keeps distances and directions from its center; the great-circle convex hull by monotone chain in a gnomonic projection; and the minimum-area rotated rectangle on a hull edge. 9 golden vectors built with GeodSolve: midpoints, equidistant rings up to 500 km, and a rectangle turned to 30°. Pending: the planar hull for projected input)
- [x] 4.4 Implement geodesic buffers with join/cap styles and measured validation; verify the geofence and collapse scenarios (`geometry.buffer.geodesic`: points, lines, and polygons with holes, positive or negative, round/mitre/bevel joins with a mitre limit, round/flat/square caps. Convex pieces on an azimuthal equidistant plane at the input's center are unioned by keeping the edges no other piece covers; vertices on round and straight parts are then placed exactly at the distance by the geodesic direct problem, and every vertex and edge midpoint is measured against the input, with the worst deviation reported and BUFFER_ACCURACY raised over tolerance. The 500 m geofence measures 0.12 m at worst; the 100 m polygon at -60 m collapses with BUFFER_COLLAPSED; circles across the antimeridian and at 89.99° N; 28 golden vectors from closed forms (Steiner's formula, rectangles, circles). The map draws the result over the input. Limited to 1,000 km from the input's center)
- [x] 4.5 Implement RDP and Visvalingam with topology preservation; verify the narrow-inlet scenario (`geometry.simplify.rdp` and `geometry.simplify.visvalingam`: vertices chosen on an azimuthal equidistant plane at the shape, RDP's tolerance then enforced by geodesic distance, the largest deviation reported on the ellipsoid, and a topology option on by default that puts vertices back until no edges cross. In the narrow-inlet scenario plain RDP cuts through a spike and the topology option restores the wall; 10 vectors match a plain-Python reference on GeographicLib GeodesicProj)
- [ ] 4.6 Implement robust predicates and point-in-polygon (winding and even-odd); verify the boundary and winding scenarios (done so far: `geometry.predicate.point-in-polygon` with geodesic edges and holes: the winding number from the signed azimuth sweep of each edge seen from the point (exact on the ellipsoid), holes turned opposite the outline, the even-odd rule as its parity, on-boundary within 1 mm by geodesic distance, and the distance to the nearest edge. Both scenarios pass (a point placed on a geodesic edge by GeodSolve is on-boundary; a pentagram's middle is winding 2, inside by the winding rule and outside by even-odd), plus a parallel's midpoint 1.2 cm outside its bowed geodesic edge, matched to Clairaut's relation within 0.01 mm; 8 golden vectors. Also `geometry.predicate.relate`, stable: the DE-9IM matrix of two shapes (point sets, lines, polygons with holes) and the eight named tests from it, intersects, disjoint, equals, within, contains, touches, crosses, and overlaps, by noding both shapes against each other and locating every node, edge piece, and both sides of every polygon edge piece in both. Planar edges (straight in longitude and latitude) use Shewchuk's exact orientation predicate; geodesic edges are cut into 5 km pieces on the ellipsoidal gnomonic plane, where they follow their chords to 0.05 mm within 1,000 km, and are on-boundary within 1 mm. It matches GEOS 3.11.4 character for character on 1,500 grid pairs chosen to be degenerate (shared edges, corners on edges, collinear overlaps, closed lines, holes), a fixture checked for teeth by three deliberate bugs; 35 golden vectors, including geodesic constructions from GeodSolve and a point inside a box with straight edges but outside it on the ground. Pending: drawing both shapes on the map, which needs a layer kind for two input shapes; a planar mode for projected coordinates rather than longitude and latitude)
- [x] 4.7 Implement validation and make-valid; verify the bow-tie scenario (`geometry.validity.make-valid`: per OGC Simple Features §6.1.11.1, every place edges cross or touch other than at a shared corner, named by ring and edge and located; repeated corners; spikes; holes outside the outline; and RFC 7946 orientation as a note. Repair keeps each piece of edge with the even-odd interior on one side only, split where edges cross, and stitches the pieces into outlines counterclockwise and holes clockwise, so a bow-tie becomes two triangles and a spike disappears. The bow-tie's crossing matches GeodSolve on its mirror meridian within 1e-8°, and the repaired area Planimeter's two triangles; 7 golden vectors. The area tool now refuses a crossing ring with DEGENERATE_GEOMETRY, its location, and a hint to this tool, with no false alarm across the 500-polygon Planimeter set)
- [ ] 4.8 Implement boolean operations (planar and geodesic-edge); verify the geofence-overlap scenario and the GEOS differential suite on JTS test geometries (done so far: `geometry.overlay.boolean` for intersection, union, difference, and symmetric difference of two polygons with holes, geodesic edges cut into 5 km pieces on one azimuthal equidistant plane; every piece of either boundary is kept exactly when the result's inside differs on its two sides, which handles shared edges and containment; results as valid rings with geodesic areas. The geofence-overlap scenario matches Planimeter on corners found by Clairaut's relation within 1e-6; 12 golden vectors including containment, disjoint, and antimeridian cases. Pending: the GEOS differential suite on JTS test geometries, and a planar mode for projected input)
- [x] 4.9 Implement densify, Delaunay, Voronoi (planar and spherical), and shape distances; verify the Fréchet scenario (done so far: `geometry.distance.tracks` with the discrete Fréchet distance by the Eiter-Mannila recurrence over geodesic distances and the index pair where it is tightest, the Hausdorff distance from each point to the other track's geodesic segments, and the closest approach (0 where tracks cross), capped at 250,000 point pairs. The Fréchet scenario puts a 30 m push at its own index pair; 6 golden vectors from GeodSolve, including resampling and reversal. Also `geometry.shape.densify`: each edge cut into equal geodesic pieces no longer than a maximum, 6 vectors from GeodSolve. And `geometry.mesh.delaunay` and `geometry.mesh.voronoi`, planar (the lifted lower hull on an azimuthal equidistant plane) and spherical (the hull of the points' directions), from one incremental 3D hull; 7 point sets match Qhull via SciPy in every triangle and cell corner)
- [x] 4.10 Enforce input limits; verify the oversized-input scenario (a 2,000,000-vertex polygon is refused with `LIMIT_EXCEEDED` by size on the web (50 MB) and MCP (10 MB) hosts before the core runs, and one that fits in bytes meets the tool's own row cap, also before any computing; every capped list refuses one row over its cap and takes the cap itself. Note: per-tool caps sit below the 1,000,000-vertex surface ceiling where the algorithm needs it, such as 10,000 corners for the area tool, whose crossing check works on at most 50,000 pieces of 5 km)

## 5. Catalog and docs

- [ ] 5.1 Register all navigation and geometry operations and generated endpoints with practitioner aliases ("as the crow flies", "great circle", "XTE", "loxodrome"); verify catalog counts (46/58 and 38/44)
- [ ] 5.2 Write docs for each tool, including a "haversine vs ellipsoid" explainer page; verify the docs build
- [ ] 5.3 Promote tools meeting the stable bar; verify the verification report
